{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Snake (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/snakegame.out",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Make: debug-full",
      "terminal": "integrated",
      "env": {
        "ASAN_OPTIONS": "detect_leaks=0"
      }
    },
    {
      "name": "Debug Snake (lldb, no ASAN)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/snakegame.out",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Make: build (no ASAN)",
      "terminal": "integrated"
    }
  ]
}